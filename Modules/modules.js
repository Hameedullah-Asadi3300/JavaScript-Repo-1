/*
modules introduced with ES6 in 2015. Module allows us to break our code into seperate files
using "export" and "import" keywords.

we have two types of export:
1. named
2. default

We do export in two ways as folows:
1. inline individually
2. all at once at end
*/



class User {
    constructor()
    {
        this.name = name
        this.password = password
        
    }
}

//  function
function firstFunction(user) {
    console.log(`My username is ${uaer.name}`);
}

export { User, firstFunction }            //  The all at once export syntax
